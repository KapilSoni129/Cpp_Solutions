#include <bits/stdc++.h>
#include <iostream>
using namespace std;

#define ll long long int
#define ld long long double
#define ss string
#define vecp vector<pair<int,int>>
#define vecs vector<pair<string,int>>
#define vec vector<int>
#define fl(i,m) for(int i=0;i<m;i++)
#define pb push_back
#define sortv(v) sort(v.begin(),v.end())
#define sort(arr,m) sort(arr,arr+m)

/*    SAAB CHUNGUS SI     */

/*    Code kya Dekh raha hai Bosdike    */
/*    Plagrism Laag Jayega    */

int main(){
    int n;cin>>n;
    while(n--){
        int m1,m2;cin>>m1>>m2;
        int m=min(m1,m2);
        //int o=max(m1,m2);
            m1=m1-m;m2=m2-m;
            for(int i=0;i<m;i++) cout<<"0"<<"1";
            for(int i=0;i<m1;i++) cout<<"0";
            for(int i=0;i<m2;i++) cout<<"1";
            cout<<"\n";
        //}
    }
return 0;
}